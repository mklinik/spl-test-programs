main() :: -> Void
{
    // ~ means the line should give a unification error
    [Int] a = 1:2:[];
    [Bool] b = True:False:[];
    [Int] c = [];
    // [Bool] ~ [Int]
    [Int] d = b:c;
    // ? this should work
    [Bool] e = True:b;

    // [a] ~ Int
    print(isEmpty(a.hd));
    // [a] ~ Bool
    print(isEmpty(True));
}
