// Each of the commented lines should give a compile error

f(x) :: Int -> Bool
{
    return False;
}

g(x,y) :: Int Int -> Int
{
    // Int ~ Bool
    return x < y;
}

h() :: -> Bool
{
    if( True )
    {
        // Bool ~ Int
        return 10;
    }
    else
    {
        return False;
    }
}


main() :: -> Void
{
    // Int ~ Bool
    print(f(True));
    // Int ~ Int -> Bool
    print(f(f));
    // Int ~ Bool
    print(f(10) + 10);
}
